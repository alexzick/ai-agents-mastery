export function btnStyle(color) {
  return {
    padding: "10px 24px",
    borderRadius: 10,
    border: `1px solid ${color}44`,
    background: color + "11",
    color: color,
    cursor: "pointer",
    fontSize: 14,
    fontWeight: 600,
    transition: "all 0.15s",
    fontFamily: "inherit",
  };
}
